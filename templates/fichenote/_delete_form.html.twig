<form method="post" action="{{ path('fichenote_delete', {'id': fichenote.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fichenote.id) }}">
    <button class="btn">Delete</button>
</form>
